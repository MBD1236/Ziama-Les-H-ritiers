{% extends "admin/base.html.twig" %}
{% block title %}
<div class="row">
    <div class="col-12">
        <div class="page-title-box">
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><i class="ri-home-4-line me-1"></i>Accueil</a></li>
                    <li class="breadcrumb-item active">Ajout</li>
                </ol>
            </div>
            <h4 class="page-title">Enregistrement des productions</h4>
        </div>
    </div>
</div>
{% endblock title %}
{% block body %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h4 class="header-title">Ajout d'un client</h4>
            </div>
            <div class="card-body">
                {{form_start(form)}}
                <div class="row">
                    <div class="col-md-6">{{ form_row(form.bobine) }}</div>
                    <div class="col-md-6">{{ form_row(form.quantiteUtilisee) }}</div>
                    <div class="col-md-6">{{ form_row(form.nombrePack) }}</div>
                    <div class="col-md-6">{{ form_row(form.dateProduction) }}</div>
                </div>
                {{form_end(form)}}
            </div>
        </div>
    </div>
</div>
{% endblock body %}
